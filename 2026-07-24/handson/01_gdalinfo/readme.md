[ハンズオン目次に戻る](../)

# 01. データ構造の確認（gdalinfo）

外部サービスに投げる前に、まず自分の目でヘッダを読みます。

## データの入手

**当日**：講師から共有するURL（当日案内。Slack等で流します）から `dem_src.tif` をダウンロードしてください。

**自宅で再現する場合（配布に依存しない経路）**：

1. 基盤地図情報ダウンロードサイト（https://fgd.gsi.go.jp/download/）で「数値標高モデル」→ 10mメッシュ（DEM10B）を選択し、対象範囲のメッシュをダウンロード（要無料アカウント）
2. 取得物はJPGIS GML（.xml）です。同梱の変換スクリプトで一括変換します：

```sh
pip install rasterio numpy
python3 fgd2tif.py FG-GML-*.zip -o dem_src.tif
```

   ダウンロードしたzipをそのまま複数渡せます（展開・パース・結合まで自動）。出力はEPSG:6668・NoData=-9999のGeoTIFFで、そのまま以降の手順に使えます。
   スクリプトを使わない場合は、QGISの変換プラグイン等でも同じものが作れます（[07_gsi1m_yamaguchi](../07_gsi1m_yamaguchi/) の変換節を参照）
3. できあがった `dem_src.tif` で以降の手順に進みます

変換まで済ませたものが当日の配布データです。自分で変換したデータと配布データで `gdalinfo` の出力を見比べるのも良い演習になります。

## 実行

```sh
gdalinfo -stats dem_src.tif
```

## 出力の読み方（確認チェックリスト）

出力例（抜粋）と、どこを見るか：

```
Driver: GTiff/GeoTIFF
Size is 4500, 3750                     <- ピクセルサイズ
Coordinate System is:
    GEOGCRS["JGD2011", ...             <- [1] CRS。地理座標か投影座標か
Pixel Size = (0.000111..., -0.000111...) <- [2] 解像度（地理座標なら「度」単位）
Band 1 Block=4500x1 Type=Float32       <- [3] データ型
  NoData Value=-9999                   <- [4] NoData値
  Minimum=1.230, Maximum=513.870       <- [5] 統計値（-statsで出る）
```

| # | 項目 | 判断すること |
|---|---|---|
| 1 | CRS | 次工程 `-s_srs` の指定値。EPSGコードを控える |
| 2 | 解像度 | 地理座標の場合、度からmへの換算感覚（緯度1秒が約30m）を持つ |
| 3 | データ型 | Float32なら標高値そのまま。Int16なら整数丸めに注意 |
| 4 | NoData | 海域・欠測部の値。後段の `-dstnodata` に引き継ぐ |
| 5 | 統計値 | 標高レンジが対象地域として妥当か。負の大きな値が混じっていたらNoData処理漏れの兆候 |

## 課題

1. 配布データのCRSのEPSGコードを特定してください
2. 標高の最小値・最大値は対象地域の実際の地形として妥当ですか
3. NoData値は何に設定されていますか（未設定の場合もあります。その場合の影響を考えてみてください）

---

前: [00. 環境準備](../00_setup/) ｜ 次: [02. 座標変換・クリップ・リサンプリング](../02_warp_clip/)
